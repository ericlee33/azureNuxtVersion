import Vue from 'vue'
import Element from 'element-ui'
import locale from 'element-ui/lib/locale/lang/zh-CN'

Vue.use(Element, { locale })
// import Vue from 'vue'

// import {Icon,Pagination,Button,Radio,Input,Message,Dialog,Notification,Form,FormItem,NavMenu,Table} from 'element-ui'

// // Vue.prototype.$loading = Loading.service
// Vue.prototype.$notify = Notification
// Vue.prototype.$message = Message

// Vue.use(Icon);

// Vue.use(Pagination);

// Vue.use(Button);
// Vue.use(Radio);
// Vue.use(Dialog)

// Vue.use(Form);

// Vue.use(FormItem);

// Vue.use(Input);
// Vue.use(NavMenu)
// Vue.use(Table)